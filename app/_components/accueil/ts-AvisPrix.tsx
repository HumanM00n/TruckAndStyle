"use client";

import { faCircleUser } from "@fortawesome/free-solid-svg-icons";
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { useRef } from "react";

export default function TsAvisPrix() {

    const scrollCard = useRef<HTMLDivElement>(null);

    // const scroll

    return (
        <section className="border-1 border-blue-500 w-full min-h-auto bg--grisArdoiseMid ">
                <div className="border flex flex-col justify-center text-white">

                    {/* NOS PRIX */}
                    <div className="">
                        
                    </div>

                    {/* COMMENTAIRES */}
                    <div className=""></div>

                </div>
        </section>  
    );
}